meta {
  name: Set System Mode (Auto)
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/admin/settings/mode
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {
    "mode": "auto"
  }
}
